# Artefactos de macOS / macOS junk
.DS_Store

# Documentos internos de trabajo del equipo (planes, implementaciones,
# revisiones). NO se publican: contienen los codigos de acceso en texto
# plano, y el candado existe justamente para que esos codigos no sean
# publicos. / Internal team handoff docs. Never published: they contain the
# access codes in plain text, and the whole point of the gate is that those
# codes stay private.
.joga/
